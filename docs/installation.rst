Installation
============

Clone the repository
--------------------

.. code-block:: bash

   git clone https://github.com/YOUR-USERNAME/YOUR-REPO.git
   cd YOUR-REPO

Install dependencies
--------------------

.. code-block:: bash

   pip install -e .

Or, if using pixi:

.. code-block:: bash

   pixi install
